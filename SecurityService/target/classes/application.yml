server:
  port: 8087
spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/micro?useSSL=false&serverTimeZone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: zxcvbnm
    driverClassName: com.mysql.cj.jdbc.Driver
  application:
    name: SECURITY-SERVICE
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
  health:
    circuitbreakers:
      enabled: true